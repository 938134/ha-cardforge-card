{
  "name": "ha-cardforge-card",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c && npm run copy-plugins",
    "copy-plugins": "cp -r src/plugins/ plugins/ || xcopy src\\plugins plugins /E /I",
    "dev": "rollup -c -w"
  },
  "devDependencies": {
    "rollup": "^4.0.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "@rollup/plugin-copy": "^3.4.0"
  }
}